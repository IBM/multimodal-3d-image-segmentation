[io]
output_dir = '~/Experiments/BraTS2023/train-0.6_valid-0.1_test-0.3/inputs'  # Where to save the output files

[partitioning]
base_paths = [  # List of str, each is a full path that contains required data.
	'~/Data/BraTS2023/ASNR-MICCAI-BraTS2023-GLI-Challenge-TrainingData/',
	]
train_fraction = 0.6  # Fraction for training in [0, 1].
valid_fraction = 0.1  # Fraction for validation in [0, 1].
test_fraction = 0.3  # Fraction for testing in [0, 1].
modalities = ['t1c', 't1n', 't2f', 't2w', 'seg']  # List of str of modalities.
ext = 'nii.gz'  # Image file extension, e.g. nii.gz
remove_str = '~/Data/BraTS2023/'  # String to be removed.
seed = 100  # For the random number generator to produce the same randomization, if not None.
